# Mobile version (GitHub Pages)

Open the mobile version at: `.../m/`

- Desktop version remains in the root `index.html` (unchanged).
- Mobile version is in `m/index.html` with mobile-only CSS overrides.
- Assets are duplicated inside `m/assets/` so it works as a standalone folder.
